{
  // Server authentication info
  "servers": [
    {
      "host": "54.183.183.71",
      "username": "ubuntu",
      // "password": "password"
      // or pem file (ssh based authentication)
      "pem": "/home/cis/Desktop/zzBase/zzBaseMeteorStaging.pem"
    }
  ],

  // Install MongoDB in the server, does not destroy local MongoDB on future setup
  "setupMongo": true,

  // WARNING: Node.js is required! Only skip if you already have Node.js installed on server.
  "setupNode": true,

  // WARNING: If nodeVersion omitted will setup 0.10.29 by default. Do not use v, only version number.
  "nodeVersion": "0.10.29",

  // Install PhantomJS in the server
  "setupPhantom": true,

  // Application name (No spaces)
  "appName": "zzBaseMeteor",

  // Location of app (local directory)
  "app": "/home/cis/Desktop/zzBase/zzbase-meteor",

  // Configure environment
  "env": {
    "PORT": 3000,
    "ROOT_URL": "https://dev.zzbase.com/",
    "MONGO_URL": "mongodb://sibananda:C!s1337@kahana.mongohq.com:10034/zzBaseMeteorStaging"
  },

  // Meteor Up checks if the app comes online just after the deployment
  // before mup checks that, it will wait for no. of seconds configured below
  "deployCheckWaitTime": 300
}


